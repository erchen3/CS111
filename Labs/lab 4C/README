NAME: ERIC CHEN
EMAIL: erchen3pro@gmail.com
ID: 305099495



lab4c_tls.c:
	C source file that sets up the TLS connection to provide secure communication with encryption back and forth between the provided server, testing the functionalities of the beaglebone.
lab4c_tcp.c:

	C source file that sets up a TCP connection similar to lab 1B in order
	to read input from the server and write it back. It tests the 
	functionalities of the beaglebone emphasizing IoT.	



Makefile:


	default:
		builds tls and tcp versions of the C source files to create an executable.
	clean:
		deletes programs generated by the makefile.
	dist:	
		builds the tarball for submission.